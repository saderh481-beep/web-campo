/**
 * DEPRECATED — Este módulo es un duplicado de usuarios.ts.
 * Usar: import { usuariosService } from './usuarios'
 *
 * Mantenido temporalmente para no romper GestionUsuariosPage.tsx.
 */
export { usuariosService as gestionUsuariosService } from './usuarios'
export type { Usuario, CreateUsuarioPayload, UpdateUsuarioPayload } from './usuarios'
